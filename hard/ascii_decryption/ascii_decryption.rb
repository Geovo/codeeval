# Let's roll with a new challenge:

# First step: count all chars (Probably inefficient, but that's just to start solving)
# Second step: check if all chars before

def decrypt(str, adder, )
  
end

File.open(ARGV[0]).each_line do |line|
  line = line.chomp
end
